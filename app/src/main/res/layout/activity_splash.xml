<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/splash"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorBg">

    <ImageView
        android:id="@+id/ivLogo"
        android:layout_width="@dimen/bytepad_logo_width"
        android:layout_height="@dimen/bytepad_logo_height"
        android:layout_centerInParent="true"
        android:src="@drawable/bytepad_icon" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/ivLogo"
        android:layout_centerHorizontal="true"
        android:padding="10dp"
        android:text="@string/app_name"
        android:textAppearance="@android:style/TextAppearance.Large"
        android:textColor="@color/textDark" />

    <com.daasuu.ahp.AnimateHorizontalProgressBar
        android:id="@+id/animate_progress_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="10dp"
        android:layout_marginEnd="5dp"
        android:layout_marginLeft="5dp"
        android:layout_marginRight="5dp"
        android:layout_marginStart="5dp"
        android:layout_marginTop="10dp"
        app:ahp_backgroundColor="@color/colorAccent"
        app:ahp_progressColor="@color/colorPrimaryDark" />


    <TextView
        android:id="@+id/tvProgressInfo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@id/animate_progress_bar"
        android:layout_centerHorizontal="true"
        android:text="Loading"
        android:textColor="@color/textDark" />

</RelativeLayout>
